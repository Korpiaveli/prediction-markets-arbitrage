{
  "name": "@arb/core",
  "version": "1.0.0",
  "description": "Core types and interfaces for arbitrage scanner",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc",
    "clean": "rimraf dist",
    "test": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}